language: cpp

compiler:
  - compiler: i586-mingw32msvc-g++

branches:
  only:
    - travis-build

env:
  - OPTIONS="-DMINGW=ON"

install:
 - sudo apt-get -qq update
 - sudo apt-get -qq install libboost-dev

script:
 - mkdir _build
 - cd _build
 - cmake ..
 - cmake --build .
